import type {SandboxForm} from "./sandboxForm.ts";

export const defaultValues = {
    testId: null,
    testField: "default",
    anotherField: "",
    thirdField: "",
    disabled: false
} as SandboxForm
